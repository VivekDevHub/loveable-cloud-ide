{
  "name": "loveable-cloud-ide",
  "version": "1.0.0",
  "private": true,
  "description": "Loveable Full-Stack Autonomous AI Web Application Platform",
  "scripts": {
    "build": "cd frontend && npm install && npm run build && cd .. && rm -rf dist && cp -r frontend/dist dist",
    "dev": "cd frontend && npm run dev"
  },
  "devDependencies": {
    "puppeteer-core": "^25.10.0",
    "vercel": "^59.16.0"
  }
}
